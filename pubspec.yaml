name: vault
description: Save key value pairs securely
version: 0.0.2
homepage: https://www.unengineered.net

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^4.2.0
  get_it: ^7.1.3

dev_dependencies:
  mocktail:
  test:
  flutter_test:
    sdk: flutter

flutter:

